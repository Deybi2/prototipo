// =============================================================================
// LAYOUT DEL MAPA - Matemáticas en Verso
// =============================================================================

import type React from "react"

export default function MapaLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
